# E2B core SDK — used only to CONNECT to existing sandboxes (no create/kill here).
e2b>=2.0.0,<3.0.0
# Official MCP SDK owns the protocol, schema generation and Streamable HTTP legs.
mcp>=2.0.0,<3.0.0
